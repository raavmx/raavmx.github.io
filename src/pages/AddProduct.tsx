import React, { FC } from 'react';
import { ProductForm } from '../components/Forms/ProductForm/ui/ProductForm';

export const AddProduct: FC = () => {
  return (
    <div>
      <ProductForm></ProductForm>
    </div>
  );
};
