import type { Meta } from '@storybook/react';
import { ThemeSwitch } from './ThemeSwitch';

const meta: Meta<typeof ThemeSwitch> = {
  title: 'Компоненты/Switches/ThemeSwitch',
  component: ThemeSwitch,
  tags: ['autodocs'],
};

export default meta;

export const themeSwitcher = {};
