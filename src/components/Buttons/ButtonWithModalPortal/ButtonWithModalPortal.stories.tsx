import type { Meta } from '@storybook/react';
import { ButtonWithModalPortal } from './ButtonWithModalPortal';

const meta: Meta<typeof ButtonWithModalPortal> = {
  title: 'Компоненты/Buttons/ButtonWithModalPortal',
  component: ButtonWithModalPortal,
  tags: ['autodocs'],
};

export default meta;

export const buttonWithModalPortal = {};
