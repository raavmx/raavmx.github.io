import type { Meta } from '@storybook/react';
import { ButtonWithInputModal } from './ButtonWithInputModal';

const meta: Meta<typeof ButtonWithInputModal> = {
  title: 'Компоненты/Buttons/ButtonWithInputModal',
  component: ButtonWithInputModal,
  tags: ['autodocs'],
};

export default meta;

export const buttonWithInputModal = {};
