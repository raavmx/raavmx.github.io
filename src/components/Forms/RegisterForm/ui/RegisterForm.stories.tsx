import type { Meta } from '@storybook/react';
import { LoginForm } from './RegisterForm';

const meta: Meta<typeof LoginForm> = {
  title: 'Компоненты/Forms/RegisterForm',
  component: LoginForm,
  tags: ['autodocs'],
  argTypes: {},
};

export default meta;
export const Primary = {};
