import type { Meta } from '@storybook/react';
import { ProfileCompleteForm } from './ProfileCompleteForm';

const meta: Meta<typeof ProfileCompleteForm> = {
  title: 'features/ProfileCompleteForm',
  component: ProfileCompleteForm,
  tags: ['autodocs'],
  argTypes: {},
};

export default meta;

export const Primary = {};
